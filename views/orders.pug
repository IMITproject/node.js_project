extends layout

block content
  h1 История заказов

  if orders.length
    style.
      table { width: 100%; border-collapse: collapse; }
      th, td { padding: 8px; text-align: left; border-bottom: 1px solid #ddd; }
      th { background-color: #f2f2f2; }
      tr:hover { background-color: #f5f5f5; }

    table
      thead
        tr
          th Заказ
          th Общая сумма
          th Товары(блюдо, количество, цена*количество)
          th Адрес
          th Телефон
          th Время доставки
      tbody
        each order in orders.reverse()
          tr
            td Заказ #{order.id}
            td #{order.total_price} руб.
            td #{order.items}
            td #{order.address}
            td #{order.phone}
            td #{order.delivery_time}
  else
    p У вас нет предыдущих заказов.

  a(href='/restaurants') Вернуться к ресторанам
  br
  a(href='/orders/cart') Вернуться в корзину